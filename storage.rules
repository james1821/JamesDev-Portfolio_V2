rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

<<<<<<< HEAD
    // Keep this in sync with firestore.rules and NUXT_PUBLIC_ADMIN_EMAIL.
    function isAdmin() {
      return request.auth != null
             && request.auth.token.email_verified == true
             && request.auth.token.email == 'jamesespinosamark@gmail.com';
=======
    function isAdmin() {
      return request.auth != null
             && firestore.exists(/databases/(default)/documents/config/admin)
             && firestore.get(/databases/(default)/documents/config/admin).data.uid == request.auth.uid;
>>>>>>> b87402ff7d3b4eb2de4ce096e6bfc16a4c5f65e7
    }

    match /images/{file} {
      allow read: if true;
      allow write: if isAdmin()
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/(jpeg|png|webp|gif)');
    }

    match /resume/{file} {
      allow read: if true;
      allow write: if isAdmin()
                   && request.resource.size < 10 * 1024 * 1024
                   && request.resource.contentType == 'application/pdf';
    }

    match /{allPaths=**} { allow read, write: if false; }
  }
}
